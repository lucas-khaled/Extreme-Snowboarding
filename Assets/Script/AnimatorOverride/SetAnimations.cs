using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ExtremeSnowboarding
{
    public class SetAnimations : MonoBehaviour
    {
        public void Set(AnimatorOverrideController overrideController, AnimatorOverrider overrider)
        {
            //if (overrideController != null)
                //overrider.SetAnimations(overrideController);
        }
    }
}
